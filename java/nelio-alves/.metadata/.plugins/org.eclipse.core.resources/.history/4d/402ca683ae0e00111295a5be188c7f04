package services;

import java.util.List;

import entities.Product;

public class ProductService {
	public Double sumByFirstLetter(List<Product> list) {
		Double sum = 0.00;
		
		for (Product p : list) {
			if (p.getName().charAt(0) == 'T') {
				sum += p.getPrice();
			}
		}
		
		return sum;
	}
}